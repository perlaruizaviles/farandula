import {SET_DATE_FIRST_FLIGHT, SET_DATE_SECOND_FLIGHT} from '../actions/types';

const dateFlights = (state, action){
  switch(aciton.type){
    case SET_DATE_FIRST_FLIGHT:
      return action.dateFirstFlight
  }
}
